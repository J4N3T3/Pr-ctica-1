#include <stdio.h>

int main() {
	int num=0, i, resultat;
	while (num<=0 || num > 100){
		printf("Introdueix un numero: ");
		scanf_s("%d", &num);
		if (num > 0 && num <= 100 ){
			break;
		}
	}
	for (i = 1; i <= 10;i++) {
		resultat = i * num;
		printf("%d x %d = %d\n", i, num, resultat);
	}
}
